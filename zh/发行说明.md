<!--
Copyright (c) 2026 Huawei Technologies Co., Ltd.
All Rights Reserved.

SPDX-License-Identifier: Apache-2.0

   Licensed under the Apache License, Version 2.0 (the "License"); you may
   not use this file except in compliance with the License. You may obtain
   a copy of the License at

        http://www.apache.org/licenses/LICENSE-2.0

   Unless required by applicable law or agreed to in writing, software
   distributed under the License is distributed on an "AS IS" BASIS, WITHOUT
   WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied. See the
   License for the specific language governing permissions and limitations
   under the License.
-->
# 发行说明

本文档是OpenAN-v26.06版本的发行说明。

# 简介

OpenAN是一个自智网络开源项目集，通过一系列开源项目支撑通信智能体的开发和部署，使能多厂商、跨层、跨域集成，加速自智网络迈向L4-L5。其核心愿景是：

- 端到端闭环自治网络：加速电信专属智能体的跨层、跨域对接集成和上线部署，多智能体高效编排与协同，使能全球运营商加速实现AN L4。

- 厂商中立与开放生态：基于行业标准（价值场景Solution Package、智能体互联协议等），推进厂商中立、产业共享的场景化“技能、组件、知识和实践”，围绕AN形成电信全产业合作生态，共同提升产业AN水平。

- 行业主导与未来演进：推动OpenAN成为电信行业事实标准，探索面向AN L5下一代智能化系统的集成与协作。

# 用户须知

OpenAN版本号计数规则是使用年月为版本号，以便用户了解版本发布时间。例如v26.06表示发布时间为2026年6月。

# 版本介绍

本版本为OpenAN的第一个版本，模块包括注册中心、编排中心、A2A-T SDK（a2a-t-sdk-python、a2a-t-sdk-java）。

- 注册中心主要功能如[表1](#table_registry_features)所示，功能特性的具体信息请参见[注册中心用户指南](https://github.com/project-openan/registry-center/blob/main/docs/zh/%E6%B3%A8%E5%86%8C%E4%B8%AD%E5%BF%83%E7%94%A8%E6%88%B7%E6%8C%87%E5%8D%97.md)。
  
- 编排中心主要功能如[表2](#table_orchestrate_features)所示，功能特性的具体信息请参见[编排中心用户指南](https://github.com/project-openan/orchestration-center/blob/main/docs/zh/%E7%94%A8%E6%88%B7%E6%8C%87%E5%8D%97.md)。
  
- a2a-t-sdk-python的主要功能如[表3](#table_a2at_sdk_features)所示，功能特性的具体信息请参见[a2a-t-sdk-python用户指南](https://github.com/project-openan/a2a-t-sdk-python/blob/main/docs/zh/%E7%94%A8%E6%88%B7%E6%8C%87%E5%8D%97.md)。
  
- a2a-t-sdk-java的主要功能如[表4](#table_a2at_java_features)所示，功能特性的具体信息请参见[a2a-t-sdk-java用户指南](https://github.com/project-openan/a2a-t-sdk-java/blob/main/docs/zh/%E7%94%A8%E6%88%B7%E6%8C%87%E5%8D%97.md)。

**表 1** 注册中心功能特性列表<a id="table_registry_features" href="#"></a>
<table border="0">
  <thead>
    <tr>
      <th>分类</th>
      <th>特性</th>
      <th>描述</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td rowspan="5">AgentCard管理</td>
      <td>AgentCard注册</td>
      <td>支持通过REST API注册第三方Agent的AgentCard信息，包含Agent名称、组织、技能描述、端点地址等元数据。</td>
    </tr>
    <tr>
      <td>AgentCard查询</td>
      <td>支持按条件查询已注册的AgentCard，包括全量查询、按ID查询详情、按名称查询等。</td>
    </tr>
    <tr>
      <td>AgentCard修改</td>
      <td>支持对已注册的AgentCard信息进行修改更新，仅允许Agent所有者进行修改操作。</td>
    </tr>
    <tr>
      <td>AgentCard删除</td>
      <td>支持从注册中心移除已注册的AgentCard，仅允许Agent所有者进行删除操作。</td>
    </tr>
    <tr>
      <td>语义检索</td>
      <td>基于Milvus向量数据库实现AgentCard的语义化检索，支持自然语言描述匹配目标Agent。</td>
    </tr>
    <tr>
      <td rowspan="4">Agent治理</td>
      <td>Agent审核</td>
      <td>提供AgentCard人工审核流程，管理员可对待注册的AgentCard进行审核通过或驳回操作，保障注册Agent的质量与合规性。</td>
    </tr>
    <tr>
      <td>标签管理</td>
      <td>支持为AgentCard设置自定义标签，便于分类和组织管理。</td>
    </tr>
    <tr>
      <td>AgentCard内容安全</td>
      <td>注册阶段自动识别并拦截包含恶意意图的AgentCard；默认校验AgentCard完整性，防止信息篡改。</td>
    </tr>
    <tr>
      <td>AgentCard签名</td>
      <td>注册中心对注册的AgentCard进行数字签名，接收方可验证AgentCard来源的真实性和完整性。</td>
    </tr>
    <tr>
      <td rowspan="2">命令行管理</td>
      <td>CLI客户端</td>
      <td>提供命令行交互界面（CLI），支持Agent查询（全量/详情）、审核（通过/驳回）、标签设置等管理操作。</td>
    </tr>
    <tr>
      <td>CLI安全输入</td>
      <td>后台命令行的敏感入参使用交互式输入，避免敏感参数被系统日志记录。</td>
    </tr>
    <tr>
      <td rowspan="3">接口服务</td>
      <td>AgentCard管理API</td>
      <td>提供AgentCard的注册、查询、修改、删除等REST接口，默认使用HTTPS协议保障通信安全。</td>
    </tr>
    <tr>
      <td>内部服务API</td>
      <td>提供面向上层编排系统的内部服务接口，支持A2A协议下的AgentCard发现与管理。</td>
    </tr>
    <tr>
      <td>验签公钥下载</td>
      <td>提供接口供外部系统下载注册中心的验签公钥，用于验证AgentCard签名的真实性。</td>
    </tr>
    <tr>
      <td rowspan="3">数据存储</td>
      <td>文件存储</td>
      <td>默认使用本地文件系统存储AgentCard数据，数据保存在{安装目录}/data/agentcard.json。</td>
    </tr>
    <tr>
      <td>PostgreSQL存储</td>
      <td>支持切换至PostgreSQL数据库持久化AgentCard数据，通过配置项persistence_mode=postgresql启用。</td>
    </tr>
    <tr>
      <td>SQLite存储</td>
      <td>支持使用SQLite嵌入式数据库进行数据持久化（开发中），通过配置项persistence_mode=sqlite启用，适用于轻量部署场景。</td>
    </tr>
    <tr>
      <td rowspan="7">安全能力</td>
      <td>TLS安全通信</td>
      <td>系统间交互默认使用HTTPS协议，支持TLSv1.3和TLSv1.2协议版本及安全的算法套件，保障通信通道安全。</td>
    </tr>
    <tr>
      <td>访问控制</td>
      <td>提供认证回调函数，供定制实现。根据Agent所有者进行AgentCard操作隔离，仅允许Agent所有者修改、删除自己的AgentCard。</td>
    </tr>
    <tr>
      <td>存储安全</td>
      <td>提供加解密回调函数，供定制实现，用于保护敏感数据存储安全。</td>
    </tr>
    <tr>
      <td>日志审计</td>
      <td>默认记录到独立日志文件，记录关键操作的6个要素；同时提供日志审计回调函数，供定制实现。</td>
    </tr>
    <tr>
      <td>证书校验</td>
      <td>支持服务端身份证书及客户端证书双向校验。证书要求：X.509v3格式，RSA（>=3072 bits）或ECDSA（>=256 bits）密钥算法，pem编码格式。</td>
    </tr>
    <tr>
      <td>吊销列表检查</td>
      <td>支持配置证书吊销列表（CRL），X.509v2格式，pem编码，在TLS握手阶段校验证书是否被吊销。</td>
    </tr>
    <tr>
      <td>证书生成工具</td>
      <td>提供独立的`generate_selfsign_cert.py`工具，用于生成自签名证书，供调试场景使用。</td>
    </tr>
    <tr>
      <td rowspan="1">可扩展性</td>
      <td>回调函数预留</td>
      <td>源码中预留认证、鉴权、加解密、密钥管理、日志审计回调函数接口，供集成方根据自身系统安全基础设施进行二次定制实现。</td>
    </tr>
  </tbody>
</table>

**表 2** 编排中心功能特性列表<a id="table_orchestrate_features" href="#"></a>
<table border="0">
  <thead>
    <tr>
      <th>分类</th>
      <th>特性</th>
      <th>描述</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td rowspan="3">工作流编排</td>
      <td>可视化编排</td>
      <td>提供图形化工作流设计器，支持通过拖拽和连线完成Agent协作流程设计，无需编写代码。</td>
    </tr>
    <tr>
      <td>多模式生成</td>
      <td>支持PDF导入、手动编排、自然语言生成三种工作流创建方式，适配不同用户习惯。</td>
    </tr>
    <tr>
      <td>智能检索</td>
      <td>基于自然语言意图检索历史工作流，快速复用已有流程，降低重复编排成本。</td>
    </tr>
    <tr>
      <td rowspan="3">流程执行</td>
      <td>动态工作流引擎</td>
      <td>基于PSOP（Plan-Specific Operation Protocol）模型驱动的执行引擎，支持线性、并行、条件分支等多种执行拓扑。</td>
    </tr>
    <tr>
      <td>实时流式执行</td>
      <td>通过SSE（Server-Sent Events）技术实时推送执行进度，包含步骤状态、Agent输出和中间结果，便于前端展示和问题定位。</td>
    </tr>
    <tr>
      <td>执行记录管理</td>
      <td>自动记录每次工作流执行的完整过程，支持根据执行ID查询执行结果和历史回溯。</td>
    </tr>
    <tr>
      <td rowspan="2">A2A-T协商</td>
      <td>Fulfillment协商</td>
      <td>集成a2a-t-sdk的fulfillment协商能力，编排端与Agent端在任务执行前进行多轮协商交互，协商上下文通过Task.metadata携带。</td>
    </tr>
    <tr>
      <td>协商上下文传递</td>
      <td>编排端自动解析Agent返回的metadata中的negotiationContext，支持协商状态的持久化和多轮协商流程。</td>
    </tr>
    <tr>
      <td rowspan="2">接口服务</td>
      <td>内部REST API</td>
      <td>提供面向前端设计器的内部API（/rest/v1/orchestrate/*），支撑工作流设计、保存、执行等前端交互。</td>
    </tr>
    <tr>
      <td>外部REST API</td>
      <td>提供面向外部系统的公共API（/api/v1/*），支持SOP编排、意图编排、自动执行、指定执行、Agent查询、结果查询等功能。</td>
    </tr>
    <tr>
      <td rowspan="2">数据存储</td>
      <td>文件存储</td>
      <td>默认使用本地文件系统存储工作流数据（PSOP、PreFlow、执行记录），数据保存在{安装目录}/data/workflow_storage目录下。</td>
    </tr>
    <tr>
      <td>PostgreSQL存储</td>
      <td>支持切换至PostgreSQL数据库持久化工作流数据，通过配置项persistence_mode=postgresql启用，启动时自动建表。</td>
    </tr>
    <tr>
      <td rowspan="2">安全通信</td>
      <td>TLS传输</td>
      <td>支持启用HTTPS协议，使用TLSv1.3和TLSv1.2协议版本，保障通信通道安全。</td>
    </tr>
    <tr>
      <td>证书校验</td>
      <td>支持配置服务端身份证书及客户端证书校验，证书格式为X.509v3，支持RSA（>=3072 bits）和ECDSA（>=256 bits）密钥算法。</td>
    </tr>
  </tbody>
</table>

**表 3** a2a-t-sdk-python功能特性列表<a id="table_a2at_sdk_features" href="#"></a>
<table border="0">
  <thead>
    <tr>
      <th>分类</th>
      <th>特性</th>
      <th>描述</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td rowspan="4">任务提示词生成</td>
      <td>场景识别</td>
      <td>基于LLM对用户输入进行场景识别，自动匹配预定义的电信运营场景（如告警订阅、故障诊断、节能优化、专线投诉等），支持中英文双语场景定义。</td>
    </tr>
    <tr>
      <td>槽位提取</td>
      <td>基于LLM从用户输入中提取结构化槽位信息，每个场景对应独立的JSON Schema槽位定义，支持必选/可选槽位校验。</td>
    </tr>
    <tr>
      <td>模板渲染</td>
      <td>支持基于场景模板的任务提示词渲染，使用<code>{{slot}}</code>占位符将提取的槽位值填充至Markdown模板，生成标准化任务提示词。</td>
    </tr>
    <tr>
      <td>元数据格式化</td>
      <td>任务提示词采用YAML front-matter格式，包含scenario_code、language、description等元数据，便于服务端解析与校验。</td>
    </tr>
    <tr>
      <td rowspan="5">任务提示词合规校验</td>
      <td>护栏检查</td>
      <td>服务端接收任务提示词后首先进行安全护栏检查，识别并拦截包含恶意意图的提示词，保障交互安全。当前提供noop（透传）实现，预留AWS Bedrock和Azure Content Safety扩展接口。</td>
    </tr>
    <tr>
      <td>场景解析</td>
      <td>自动解析任务提示词的front-matter元数据，识别scenario_code并加载对应的槽位Schema和场景模板。</td>
    </tr>
    <tr>
      <td>槽位校验</td>
      <td>基于JSON Schema对提取的槽位值进行结构化校验，确保槽位值的类型、格式、必选约束均符合场景定义。</td>
    </tr>
    <tr>
      <td>语义校验</td>
      <td>基于LLM对槽位值进行语义层面的合理性校验，检测逻辑矛盾或不符合业务语义的值，校验为可选项。</td>
    </tr>
    <tr>
      <td>合规流水线</td>
      <td>服务端提供完整的合规校验流水线（护栏→场景解析→槽位提取→Schema校验→语义校验），可通过配置项按需启停各环节。</td>
    </tr>
    <tr>
      <td rowspan="4">多轮协商</td>
      <td>信息协商（Information）</td>
      <td>支持Agent间信息补充协商，当任务提示词信息不完整时，服务端可发起信息协商请求获取缺失信息。</td>
    </tr>
    <tr>
      <td>澄清协商（Clarification）</td>
      <td>支持Agent间需求澄清协商，当任务意图模糊或存在歧义时，通过多轮交互明确具体需求。</td>
    </tr>
    <tr>
      <td>可行性协商（Feasibility）</td>
      <td>支持Agent间可行性确认协商，在任务执行前评估可行性，避免不可行任务的无效执行。</td>
    </tr>
    <tr>
      <td>履行协商（Fulfillment）</td>
      <td>支持Agent间任务执行细节协商，协商任务执行方案、时间、资源等细节，达成一致后进入执行阶段。</td>
    </tr>
    <tr>
      <td rowspan="3">协商状态管理</td>
      <td>状态机驱动</td>
      <td>每种协商类型均由状态机驱动，支持IN_PROGRESS、AGREED、REJECTED三种状态流转，自动跟踪协商轮次并限制最大轮次防止无限循环。</td>
    </tr>
    <tr>
      <td>上下文传递</td>
      <td>协商上下文支持序列化与反序列化，可通过Task.metadata在Agent间传递，保障多轮协商状态的连续性。</td>
    </tr>
    <tr>
      <td>状态存储</td>
      <td>提供NegotiationStateStore协议接口，当前内置内存存储实现，支持按会话ID存取协商状态。</td>
    </tr>
    <tr>
      <td rowspan="4">LLM运行时</td>
      <td>多模式调用</td>
      <td>支持三种LLM调用模式：单次补全（complete）、带会话历史的对话（chat）、结构化输出（structured），满足不同场景需求。</td>
    </tr>
    <tr>
      <td>适配器工厂</td>
      <td>提供LLMAdapterFactory工厂类，支持直接注册适配器和组合式注册（transport + payload_builder + response_parser），当前预注册deepseek（OpenAI兼容协议）适配器。</td>
    </tr>
    <tr>
      <td>会话管理</td>
      <td>内置会话历史管理，支持配置历史窗口大小（A2AT_LLM_HISTORY_WINDOW）、最大会话总数和单Provider会话上限，防止内存溢出。</td>
    </tr>
    <tr>
      <td>可扩展</td>
      <td>LLM适配器支持动态注册，兼容所有OpenAI协议的模型服务均可通过配置接入，无需修改SDK源码。</td>
    </tr>
    <tr>
      <td rowspan="3">提示词资源管理</td>
      <td>多语言资源</td>
      <td>提示词资源按语言目录组织，支持中文（zh-CN）和英文（en-US）双语，涵盖场景定义、槽位Schema、模板和系统/用户提示词。</td>
    </tr>
    <tr>
      <td>自定义资源目录</td>
      <td>支持通过A2AT_PROMPT_RESOURCE_LOCAL_ROOT_DIR配置项指定自定义资源根目录，加载自定义场景、槽位和模板，包内资源作为兜底。</td>
    </tr>
    <tr>
      <td>标准化组织</td>
      <td>提示词资源采用标准化目录结构组织：scenarios/{lang}/scenarios.json（场景定义）、slots/{scenario}/{lang}/slot.json（槽位Schema）、templates/{scenario}/{lang}/template.md（模板）、prompts/{action}/{lang}/（系统/用户提示词）。</td>
    </tr>
    <tr>
      <td rowspan="2">配置管理</td>
      <td>环境变量配置</td>
      <td>所有配置项通过.env文件管理，支持提示词运行时、合规校验、LLM运行时、协商四大类配置，提供env.example模板文件。</td>
    </tr>
    <tr>
      <td>配置分层</td>
      <td>支持包级默认配置与用户自定义配置分层，用户仅需覆盖需要修改的配置项，其余使用默认值。</td>
    </tr>
  </tbody>
</table>

**表 4** a2a-t-sdk-java功能特性列表<a id="table_a2at_java_features" href="#"></a>
<table border="0">
  <thead>
    <tr>
      <th>分类</th>
      <th>特性</th>
      <th>描述</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td rowspan="5">任务提示词生成</td>
      <td>场景识别</td>
      <td>基于LLM对用户输入进行场景识别，自动匹配预定义的电信运营场景（如告警订阅、故障诊断、节能优化、专线投诉等），支持中英文双语场景定义。</td>
    </tr>
    <tr>
      <td>槽位提取</td>
      <td>基于LLM从用户输入中提取结构化槽位信息，每个场景对应独立的JSON Schema槽位定义（<code>ClientSlotJsonSchema</code>），支持必选/可选槽位校验。</td>
    </tr>
    <tr>
      <td>模板渲染</td>
      <td>支持基于场景模板的任务提示词渲染，使用<code>{{variable}}</code>占位符将提取的槽位值填充至Markdown模板，生成标准化任务提示词。模板分为L0基础模板（Task-T、Notification-T）和L1场景模板两级层次。</td>
    </tr>
    <tr>
      <td>元数据格式化</td>
      <td>任务提示词采用YAML front-matter格式，包含scenario_code、language、description等元数据，便于服务端解析与校验。</td>
    </tr>
    <tr>
      <td>编排器模式</td>
      <td>客户端提示词生成由<code>ClientPromptGenerationOrchestrator</code>编排器驱动，提供默认实现<code>DefaultClientPromptGenerationOrchestrator</code>，支持通过Builder模式自定义替换各环节组件。</td>
    </tr>
    <tr>
      <td rowspan="6">任务提示词合规校验</td>
      <td>护栏检查</td>
      <td>服务端接收任务提示词后首先进行安全护栏检查，识别并拦截包含恶意意图的提示词，保障交互安全。提供<code>ServerPromptGuardrail</code>扩展接口和<code>NoopServerPromptGuardrail</code>默认透传实现，预留AWS Bedrock和Azure Content Safety外部策略端点配置。</td>
    </tr>
    <tr>
      <td>场景解析</td>
      <td>自动解析任务提示词的front-matter元数据，识别scenario_code并加载对应的槽位Schema和场景模板。支持LLM解析（<code>LlmBackedPromptMetadataExtractor</code>）和模板匹配解析（<code>TemplateMatchingPromptMetadataExtractor</code>）两种策略。</td>
    </tr>
    <tr>
      <td>槽位校验</td>
      <td>基于JSON Schema对提取的槽位值进行结构化校验，确保槽位值的类型、格式、必选约束均符合场景定义。</td>
    </tr>
    <tr>
      <td>语义校验</td>
      <td>支持两种语义校验策略：LLM语义校验（<code>LlmBackedPromptSemanticValidator</code>）和模板往返校验（<code>TemplateRoundTripPromptSemanticValidator</code>），检测逻辑矛盾或不符合业务语义的值。</td>
    </tr>
    <tr>
      <td>合规流水线</td>
      <td>服务端提供完整的合规校验流水线（护栏→场景解析→槽位提取→Schema校验→语义校验），由<code>ServerPromptComplianceOrchestrator</code>编排器驱动，可通过配置项按需启停各环节。</td>
    </tr>
    <tr>
      <td>合规结果模型</td>
      <td>提供<code>PromptComplianceResult</code>和<code>PromptComplianceFailure</code>结构化结果模型，明确记录校验失败环节（stage）和错误码，便于问题定位。</td>
    </tr>
    <tr>
      <td rowspan="4">多轮协商</td>
      <td>信息协商（Information）</td>
      <td>支持Agent间信息补充协商，当任务提示词信息不完整时，服务端可发起信息协商请求获取缺失信息。</td>
    </tr>
    <tr>
      <td>澄清协商（Clarification）</td>
      <td>支持Agent间需求澄清协商，当任务意图模糊或存在歧义时，通过多轮交互明确具体需求。</td>
    </tr>
    <tr>
      <td>可行性协商（Feasibility）</td>
      <td>支持Agent间可行性确认协商，在任务执行前评估可行性，避免不可行任务的无效执行。</td>
    </tr>
    <tr>
      <td>履行协商（Fulfillment）</td>
      <td>支持Agent间任务执行细节协商，协商任务执行方案、时间、资源等细节，达成一致后进入执行阶段。</td>
    </tr>
    <tr>
      <td rowspan="4">协商状态管理</td>
      <td>状态机驱动</td>
      <td>每种协商类型均由状态机驱动，支持IN_PROGRESS、AGREED、REJECTED三种状态流转，自动跟踪协商轮次并限制最大轮次防止无限循环。轮次不匹配时抛出<code>NegotiationStateException</code>。</td>
    </tr>
    <tr>
      <td>上下文传递</td>
      <td>协商上下文遵循A2A-T协议扩展URI约定，通过<code>https://projects.tmforum.org/a2aproject/telecommunication/extensions/DATA-NEGOTIATION-T/v1</code>键在Task.metadata中携带，支持序列化与反序列化，保障多轮协商状态连续性。</td>
    </tr>
    <tr>
      <td>状态存储</td>
      <td>提供<code>NegotiationStore</code>接口，当前内置<code>InMemoryNegotiationStore</code>内存实现，支持按会话ID存取协商状态，可扩展持久化实现。</td>
    </tr>
    <tr>
      <td>Handler注册模式</td>
      <td>提供<code>NegotiationHandler</code>核心处理器，支持Builder模式注册协商类型处理器（<code>NegotiationTypeHandler</code>）和状态存储，客户端与服务端通过<code>RoleBoundNegotiationOrchestrator</code>共享协商编排逻辑。</td>
    </tr>
    <tr>
      <td rowspan="4">LLM运行时</td>
      <td>适配器架构</td>
      <td>提供<code>LLMAdapter</code>接口和<code>OpenAICompatibleAdapter</code>实现，基于OpenAI Java SDK（v4.36.0）兼容所有OpenAI协议的模型服务，通过配置即可切换不同LLM后端。</td>
    </tr>
    <tr>
      <td>结构化生成</td>
      <td>支持JSON Schema约束的结构化输出（<code>StructuredGenerationRequest</code>），用于场景识别、槽位提取等需要结构化响应的LLM调用场景。</td>
    </tr>
    <tr>
      <td>会话管理</td>
      <td>内置<code>LlmSessionStore</code>和<code>InMemoryLlmSessionStore</code>，支持配置历史窗口大小、最大会话总数和单Provider会话上限，防止内存溢出。提供Token使用量追踪（<code>LlmUsage</code>）。</td>
    </tr>
    <tr>
      <td>本地规则引擎</td>
      <td>除OpenAI兼容适配器外，提供<code>local_rule</code>提供者类型，支持基于规则的本地场景识别与槽位提取，适用于无需LLM的确定性场景。</td>
    </tr>
    <tr>
      <td rowspan="3">提示词资源管理</td>
      <td>多语言资源</td>
      <td>提示词资源按语言目录组织，支持中文（zh-CN）和英文（en-US）双语，涵盖场景定义、槽位Schema、模板和系统/用户提示词。</td>
    </tr>
    <tr>
      <td>多源加载</td>
      <td>支持Classpath资源加载（<code>ClasspathPromptResourceLoader</code>）和本地文件系统加载（<code>LocalFilePromptTemplateLoader</code>等），包内Classpath资源作为兜底，用户可通过配置指定自定义资源目录。</td>
    </tr>
    <tr>
      <td>标准化组织</td>
      <td>提示词资源采用标准化目录结构组织：scenarios/{lang}/scenarios.json（场景定义）、slots/{scenario}/{lang}/slot.json（槽位Schema）、templates/{scenario}/{lang}/template.md（模板）、prompts/{action}/{lang}/（系统/用户提示词）。</td>
    </tr>
    <tr>
      <td rowspan="3">配置管理</td>
      <td>环境变量配置</td>
      <td>所有配置项通过.env文件管理，SDK不自动发现.env文件，需由调用方显式传入路径。支持提示词运行时、合规校验、LLM运行时、协商四大类配置。</td>
    </tr>
    <tr>
      <td>结构化配置模型</td>
      <td>提供<code>A2ATConfig</code>、<code>PromptRuntimeConfig</code>、<code>LlmConfig</code>、<code>NegotiationConfig</code>、<code>PromptComplianceConfig</code>等Record类型配置模型，类型安全且不可变。</td>
    </tr>
    <tr>
      <td>BOM版本管理</td>
      <td>提供<code>a2a-t-bom</code> Bill of Materials模块，集成方通过引入BOM即可统一管理所有a2a-t模块的版本，避免版本冲突。</td>
    </tr>
    <tr>
      <td rowspan="2">示例集成</td>
      <td>端到端示例</td>
      <td>提供<code>a2a-t-sample</code>模块，演示完整的客户端-服务端交互流程，集成A2A Java SDK（v1.0.0.Beta1）实现HTTP传输，包含AgentCard注册、任务提示词生成与校验、SSE流式事件推送等完整场景。</td>
    </tr>
    <tr>
      <td>Builder构建模式</td>
      <td>客户端<code>A2ATClient</code>和服务端<code>A2ATServer</code>均支持通过Builder模式（<code>DefaultA2ATClientBuilder</code>、<code>DefaultA2ATServerBuilder</code>）自定义组装各组件，便于集成方按需替换实现。</td>
    </tr>
  </tbody>
</table>

# 版本配套说明

## 注册中心

### 发布件

注册中心首次发行仅提供源码，不提供二进制安装软件包。源码可从[OpenAN社区注册中心代码仓](https://github.com/project-openan/registry-center)获取。

**表 1** 注册中心v1.0.0版本发布件列表

<table border="0">
  <thead>
    <tr>
      <th>交付形式</th>
      <th>名称</th>
      <th>描述</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>源码</td>
      <td>registry-center源码</td>
      <td>注册中心完整源代码，包含AgentCard管理、CLI客户端、安全能力等模块</td>
    </tr>
  </tbody>
</table>

> **说明：**
>
> - 首次发行仅交付源码，交付内容不包括构建工程及二进制安装包。
> - 使用方需自行完成构建及部署安装，安装完成后须保证各文件及目录权限最小化（文件权限400、目录权限700、可执行.sh文件权限500）。
> - 仅{安装目录}/etc下的文件需要可写权限，其他文件均只有可读权限。

### 操作系统版本配套说明

**表 2** 注册中心v1.0.0版本支持场景

| 操作系统 | 版本 | 架构 | 适用说明 |
|---------|------|------|----------|
| openEuler | 22.03 LTS SP3 | x86_64、ARM64 | 生产环境推荐 |
| Ubuntu | 22.04 | x86_64、ARM64 | 生产环境可用 |
| Windows | Windows 10/11 | x86_64 | 仅限开发调试 |

> **说明：**
>
> - 本项目须运行在Linux操作系统上，支持IPv4网络环境。
> - Windows环境仅用于开发调试，不支持生产部署。
> - 仅支持单实例部署，须作为内部系统使用，不可开放到公网，不可作为云服务部署。

### 运行时环境版本配套说明

**表 3** 注册中心v1.0.0版本运行时环境要求

| 软件 | 版本要求 | 用途 |
|-----|---------|------|
| Python | >= 3.12 | 注册中心服务运行环境 |
| PostgreSQL（可选） | >= 12 | 数据持久化数据库，文件存储或SQLite存储模式下非必需 |
| Milvus（可选） | >= 2.6 | 语义检索能力依赖的向量数据库 |

> **说明：**
>
> - 注册中心服务通过`python -m agent_registry.start`启动，默认侦听`127.0.0.1:5000`。
> - IP及端口可按需修改，配置文件为{安装目录}/etc/conf/server.conf。
> - 默认使用文件存储模式（persistence_mode=file），数据保存在{安装目录}/data/agentcard.json。支持切换至postgresql或sqlite（开发中）存储模式，配置文件为{安装目录}/etc/conf/persistence.conf。
> - Milvus为语义检索功能的可选依赖，如不使用语义检索可不部署。

### 核心依赖版本配套说明

**表 4** 注册中心v1.0.0版本核心Python依赖

| 依赖 | 版本 | 用途 |
|-----|------|------|
| a2a-sdk | >= 1.0.0a1 | A2A协议实现 |
| a2a-sdk[signing] | >= 1.0.0a1 | AgentCard签名能力 |
| fastapi | >= 0.115.11 | REST API框架 |
| uvicorn | >= 0.34.0 | ASGI服务器 |
| loguru | >= 0.7.3 | 日志记录 |
| cryptography | ~= 46.0.5 | 加密算法支持 |
| pymilvus | ~= 2.6.12 | Milvus向量数据库客户端（语义检索） |
| PyJWT | ~= 2.10.1 | JWT令牌处理 |
| requests | ~= 2.32.3 | HTTP客户端 |
| httpx | ~= 0.28.1 | 异步HTTP客户端 |
| environs | ~= 15.0.1 | 环境变量解析 |
| limits | ~= 4.0.0 | 速率限制 |
| starlette | ~= 0.50.0 | ASGI框架核心 |

> **说明：**
>
> - 完整依赖列表见requirements.txt。
> - a2a-sdk[signing]提供AgentCard签名与验签能力，注册中心对注册的AgentCard进行数字签名。
> - 注册中心自身不提供登录认证、鉴权、用户管理、加解密、密钥管理能力，需由集成方系统提供上述安全基础设施。
> - 证书要求：身份证书server.cer（必选，pem编码，X.509v3格式），私钥server_key.pem（必选，pem编码），私钥口令cert_pwd（必选，密文存储），信任证书trust.cer（verify_client=true时必选），吊销列表revocationlist.crl（可选）。
> - 证书校验失败将导致进程拉起失败；证书变更后须重启进程生效。

## 编排中心

### 发布件

编排中心首次发行仅提供源码，不提供二进制安装软件包。源码可从[OpenAN社区编排中心代码仓](https://github.com/project-openan/orchestration-center/tree/main)获取。

**表 1** 编排中心v1.0.0版本发布件列表

<table border="0">
  <thead>
    <tr>
      <th>交付形式</th>
      <th>名称</th>
      <th>描述</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>源码</td>
      <td>orchestration-center源码</td>
      <td>编排中心完整源代码，包含后端编排引擎、前端工作流设计器及示例Agent</td>
    </tr>
  </tbody>
</table>

> **说明：**
>
> - 首次发行仅交付源码，交付内容不包括构建工程及二进制安装包。
> - 使用方需自行完成构建及部署安装。

### 操作系统版本配套说明

**表 2** 编排中心v1.0.0版本支持场景

| 操作系统 | 版本 | 架构 | 适用说明 |
|---------|------|------|----------|
| openEuler | 22.03 LTS SP3 | x86_64、ARM64 | 生产环境推荐 |
| Ubuntu | 22.04 | x86_64、ARM64 | 生产环境可用 |
| Windows | Windows 10/11 | x86_64 | 仅限开发调试 |

> **说明：**
>
> - 生产环境须部署在Linux操作系统上。
> - Windows环境仅用于开发调试，不支持生产部署。
> - 不支持同一集群内不同OS混部。

### 运行时环境版本配套说明

**表 3** 编排中心v1.0.0版本运行时环境要求

| 软件 | 版本要求 | 用途 |
|-----|---------|------|
| Python | >= 3.12 | 后端编排引擎运行环境 |
| Node.js | >= 20.19 | 前端工作流设计器运行环境 |
| PostgreSQL（可选） | >= 12 | 数据持久化数据库，文件存储模式下非必需 |

> **说明：**
>
> - 后端服务通过`python -m orchestrate.start`启动，默认侦听`127.0.0.1:5001`。
> - 前端服务通过`npm run dev`启动，默认侦听`localhost:3003`。
> - 示例Agent通过`python -m samples.start_agents_server`启动，为可选组件，但工作流执行须依赖示例Agent提供A2A Agent端点。
> - 默认使用文件存储模式（persistence_mode=file），数据保存在{安装目录}/data/workflow_storage目录下。如需切换至PostgreSQL，须配置persistence_mode=postgresql并修改{安装目录}/etc/conf/db_config.json中的数据库连接信息。

### 核心依赖版本配套说明

**表 4** 编排中心v1.0.0版本核心Python依赖

| 依赖 | 版本 | 用途 |
|-----|------|------|
| a2a-t-sdk | >= 1.0.0 | Agent协商能力（fulfillment协商） |
| a2a-sdk | latest | A2A协议实现（http-server + grpc） |
| fastapi | >= 0.135.1 | REST API框架 |
| uvicorn | >= 0.42 | ASGI服务器 |
| pydantic | >= 2.12.5 | 数据模型验证 |
| openai | >= 2.26.0 | LLM调用 |
| loguru | >= 0.7.3 | 日志记录 |
| PyYAML | >= 6.0.3 | YAML解析 |
| pymupdf | latest | PDF文档解析 |

> **说明：**
>
> - 完整依赖列表见requirements.txt。
> - a2a-t-sdk的协商配置由`common/config/llm_config.json`自动生成至`samples/a2at_config/.env`。

## a2a-t-sdk-python

### 发布件

a2a-t-sdk-python首次发行仅提供源码，不提供二进制安装软件包。源码可从[OpenAN社区a2a-t-sdk-python代码仓](https://github.com/project-openan/a2a-t-sdk-python)获取。

**表 1** a2a-t-sdk-python v1.0.0版本发布件列表

<table border="0">
  <thead>
    <tr>
      <th>交付形式</th>
      <th>名称</th>
      <th>描述</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>源码</td>
      <td>a2a-t-sdk-python源码</td>
      <td>A2A-T SDK完整源代码，包含客户端任务提示词生成、服务端合规校验、多轮协商、LLM运行时及提示词资源管理等模块</td>
    </tr>
  </tbody>
</table>


> **说明：**
>
> - 首次发行仅交付源码，交付内容不包括构建工程及二进制安装包。
> - 使用方需通过`uv sync --dev`或`pip install -e .`自行完成构建及安装。
> - 本版本为Alpha阶段（v1.0.0），接口和资源组织方式可能随后续版本演进调整。

### 操作系统版本配套说明

**表 2** a2a-t-sdk-python v1.0.0版本支持场景

| 操作系统 | 版本 | 架构 | 适用说明 |
|---------|------|------|----------|
| openEuler | 22.03 LTS SP3 | x86_64、ARM64 | 生产环境推荐 |
| Ubuntu | 22.04 / 24.04 | x86_64、ARM64 | 生产环境可用 |
| macOS | 13+ | x86_64、Apple Silicon | 开发调试 |
| Windows | Windows 10/11 | x86_64 | 仅限开发调试 |

> **说明：**
>
> - a2a-t-sdk-python为纯Python包，无平台原生依赖，理论上支持所有Python 3.12+可运行的平台。
> - Windows和macOS环境仅用于开发调试，不支持生产部署。
> - 本SDK自身不提供HTTP服务，需由集成方业务系统提供传输层和部署环境。

### 运行时环境版本配套说明

**表 3** a2a-t-sdk-python v1.0.0版本运行时环境要求

| 软件 | 版本要求 | 用途 |
|-----|---------|------|
| Python | >= 3.12 | SDK运行环境 |
| uv（推荐）/ pip | uv >= 0.4 / pip >= 23 | 包管理与构建工具 |
| LLM服务 | 兼容OpenAI协议 | 任务提示词生成、槽位提取、语义校验等LLM调用场景依赖 |

> **说明：**
>
> - 推荐使用uv进行包管理，通过`uv sync --dev`安装依赖；也可使用`pip install -e .`安装。
> - LLM服务为必选依赖，SDK的所有LLM相关功能（场景识别、槽位提取、语义校验、协商交互）均依赖外部LLM服务。
> - 默认预注册deepseek适配器（兼容OpenAI协议），可通过`A2AT_LLM_BASE_URL`和`A2AT_LLM_API_KEY`配置接入其他兼容OpenAI协议的模型服务（如OpenAI、通义千问等）。
> - 配置文件模板见`package_data/env.example`，使用时复制为`.env`文件并按需修改。

### 核心依赖版本配套说明

**表 4** a2a-t-sdk-python v1.0.0版本核心Python依赖

| 依赖 | 版本 | 用途 |
|-----|------|------|
| openai | latest | OpenAI兼容协议LLM客户端，用于场景识别、槽位提取、语义校验、协商交互等LLM调用 |
| jsonschema | >= 4.23.0 | JSON Schema校验，用于服务端槽位值的结构化校验 |
| python-dotenv | >= 1.1.0 | 环境变量加载，用于从.env文件读取SDK配置 |

> **说明：**
>
> - 完整依赖列表见`pyproject.toml`。
> - openai库为LLM调用的核心依赖，通过其兼容协议支持多种模型服务后端。
> - jsonschema用于服务端合规校验流水线中的槽位Schema校验环节。
> - python-dotenv负责SDK启动时从.env文件加载配置，所有配置项均通过环境变量注入。
> - 开发依赖包括：pytest（>= 8.0.0，测试框架）、ruff（>= 0.3.0，代码检查与格式化）、mypy（>= 1.9.0，类型检查）。

### 配置项配套说明

**表 5** a2a-t-sdk-python v1.0.0版本配置项说明

| 配置分类 | 配置项 | 默认值 | 说明 |
|---------|-------|-------|------|
| 提示词运行时 | A2AT_LANGUAGE | en-US | 提示词资源语言，支持zh-CN、en-US |
| 提示词运行时 | A2AT_PROMPT_SOURCE_TYPE | local_file | 提示词资源来源类型 |
| 提示词运行时 | A2AT_PROMPT_RESOURCE_LOCAL_ROOT_DIR | 包内默认目录 | 自定义资源根目录路径 |
| 合规校验 | A2AT_PROMPT_COMPLIANCE_ENABLED | false | 是否启用服务端合规校验 |
| 合规校验 | A2AT_PROMPT_COMPLIANCE_GUARDRAIL_PROVIDER | noop | 护栏提供者（当前仅noop） |
| 合规校验 | A2AT_PROMPT_COMPLIANCE_GUARDRAIL_TIMEOUT_SECONDS | 10 | 护栏请求超时（秒） |
| LLM运行时 | A2AT_LLM_PROVIDER | deepseek | LLM提供者名称 |
| LLM运行时 | A2AT_LLM_MODEL | deepseek-chat | 模型名称 |
| LLM运行时 | A2AT_LLM_API_KEY | （空） | API密钥（必填） |
| LLM运行时 | A2AT_LLM_BASE_URL | https://api.deepseek.com | API基础地址 |
| LLM运行时 | A2AT_LLM_MAX_TOKENS | 2000 | 最大Token数 |
| LLM运行时 | A2AT_LLM_TEMPERATURE | 0 | 采样温度 |
| LLM运行时 | A2AT_LLM_TIMEOUT_SECONDS | 60 | 请求超时（秒） |
| LLM运行时 | A2AT_LLM_HISTORY_WINDOW | 10 | 对话历史窗口消息数 |
| LLM运行时 | A2AT_LLM_SESSION_MAX_TOTAL | 300 | 最大会话总数 |
| LLM运行时 | A2AT_LLM_SESSION_MAX_PER_PROVIDER | 100 | 单Provider最大会话数 |
| 协商 | A2AT_NEGOTIATION_STATE_STORE_TYPE | in_memory | 协商状态存储类型 |

> **说明：**
>
> - 所有配置项通过.env文件管理，配置模板见`package_data/env.example`。
> - A2AT_LLM_API_KEY为必填项，须配置有效的LLM服务API密钥。
> - A2AT_LLM_BASE_URL可替换为任意兼容OpenAI协议的服务端点，接入不同的模型服务仅需修改此配置和对应的API_KEY。
> - 合规校验相关配置仅影响服务端行为，客户端无需关注。
> - A2AT_PROMPT_COMPLIANCE_GUARDRAIL_PROVIDER当前仅支持noop（透传），aws_bedrock和azure_content_safety为预留扩展接口，暂未实现。

### 协议标准配套说明

**表 6** a2a-t-sdk-python v1.0.0版本遵循的协议标准

| 标准 | 版本 | 说明 |
|-----|------|------|
| IG1453 A Structured Prompt of Agent to Agent Protocol for Telecoms (A2AT) | v1.0.0 | TM Forum发布的A2A-T结构化提示词协议标准 |
| IG1453 Agent to Agent Protocol for Telecoms (A2AT) | v2.0.0 | TM Forum发布的A2A-T协议标准，涵盖协商扩展 |

> **说明：**
>
> - a2a-t-sdk-python为A2A-T协议标准的参考实现SDK，任务提示词格式和协商流程遵循上述协议标准。
> - A2A-T协议是A2A（Agent-to-Agent）协议在电信领域的扩展，由TM Forum标准化发布。


## a2a-t-sdk-java

### 发布件

a2a-t-sdk-java首次发行仅提供源码，不提供二进制安装软件包。源码可从[OpenAN社区a2a-t-sdk-java代码仓](https://github.com/project-openan/a2a-t-sdk-java)。

**表 1** a2a-t-sdk-java v1.0.0版本发布件列表

<table border="0">
  <thead>
    <tr>
      <th>交付形式</th>
      <th>名称</th>
      <th>描述</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>源码</td>
      <td>a2a-t-java源码</td>
      <td>a2a-t-sdk-java完整源代码，包含核心模型、资源管理、LLM运行时、提示词处理、协商运行时、客户端门面、服务端门面及示例集成等9个Maven模块</td>
    </tr>
    <tr>
      <td>Maven制品</td>
      <td>a2a-t-bom</td>
      <td>Bill of Materials，用于统一管理A2A-T模块版本依赖</td>
    </tr>
    <tr>
      <td>Maven制品</td>
      <td>a2a-t-client</td>
      <td>客户端SDK模块，提供A2ATClient门面及任务提示词生成能力</td>
    </tr>
    <tr>
      <td>Maven制品</td>
      <td>a2a-t-server</td>
      <td>服务端SDK模块，提供A2ATServer门面及任务提示词合规校验能力</td>
    </tr>
  </tbody>
</table>

> **说明：**
>
> - 首次发行仅交付源码，集成方需通过Maven构建生成制品。
> - 使用方可通过Maven依赖引入SDK，也可通过`mvn -DskipTests package`自行构建。
> - 本版本为Alpha阶段（v1.0.0），接口和模块组织方式可能随后续版本演进调整。

### 操作系统版本配套说明

**表 2** a2a-t-sdk-java v1.0.0版本支持场景

| 操作系统 | 版本 | 架构 | 适用说明 |
|---------|------|------|----------|
| openEuler | 22.03 LTS SP3 | x86_64、ARM64 | 生产环境推荐 |
| Ubuntu | 22.04 / 24.04 | x86_64、ARM64 | 生产环境可用 |
| macOS | 13+ | x86_64、Apple Silicon | 开发调试 |
| Windows | Windows 10/11 | x86_64 | 仅限开发调试 |

> **说明：**
>
> - a2a-t-sdk-java为纯Java SDK，无平台原生依赖，理论上支持所有JDK 17+可运行的平台。
> - Windows和macOS环境仅用于开发调试，不支持生产部署。
> - 本SDK自身不提供HTTP服务，需由集成方业务系统提供传输层和部署环境。

### 运行时环境版本配套说明

**表 3** a2a-t-sdk-java v1.0.0版本运行时环境要求

| 软件 | 版本要求 | 用途 |
|-----|---------|------|
| JDK | >= 17 | SDK运行环境 |
| Maven | >= 3.8 | 构建与依赖管理工具 |
| LLM服务 | 兼容OpenAI协议 | 场景识别、槽位提取、语义校验等LLM调用场景依赖 |

> **说明：**
>
> - JDK 17为最低版本要求，推荐使用LTS版本（JDK 17或JDK 21）。
> - 构建命令为`mvn -DskipTests package`，运行测试使用`mvn test`。
> - LLM服务为必选依赖，SDK的所有LLM相关功能（场景识别、槽位提取、语义校验、协商交互）均依赖外部LLM服务。
> - 默认预注册openai_compatible适配器，可通过`A2AT_LLM_BASE_URL`和`A2AT_LLM_API_KEY`配置接入兼容OpenAI协议的模型服务（如OpenAI、Azure OpenAI、DeepSeek、通义千问等）。
> - 配置文件模板见`package_data/env.example`，使用时复制为`.env`文件并按需修改。SDK不自动发现.env文件，需由调用方显式传入路径。

### 核心依赖版本配套说明

**表 4** a2a-t-sdk-java v1.0.0版本核心依赖

| 依赖 | 版本 | 用途 |
|-----|------|------|
| Lombok | 1.18.38 | 代码生成（注解处理器，provided scope） |
| Jackson Databind | 2.20.1 | JSON序列化与反序列化 |
| Jackson Annotations | 2.20.1 | Jackson注解支持 |
| Jackson Core | 2.20.1 | Jackson核心解析器 |
| OpenAI Java SDK (okhttp) | 4.36.0 | OpenAI兼容协议LLM客户端 |
| OpenAI Java Core | 4.36.0 | OpenAI Java SDK核心模块 |
| JUnit Jupiter | 5.10.2 | 单元测试框架 |

> **说明：**
>
> - 完整依赖列表见各模块`pom.xml`。
> - Lombok为编译期依赖（provided scope），运行时不需要。
> - OpenAI Java SDK为LLM调用的核心依赖，通过其兼容协议支持多种模型服务后端。
> - A2A Java SDK（v1.0.0.Beta1）仅用于`a2a-t-sample`示例模块，非SDK运行时必需依赖。

### 模块版本配套说明

**表 5** a2a-t-sdk-java v1.0.0版本模块列表

| 模块 | ArtifactId | 用途 | 依赖模块 |
|------|-----------|------|----------|
| BOM | a2a-t-bom | 版本统一管理，集成方通过引入BOM统一管理所有模块版本 | 无 |
| 核心 | a2a-t-core | 共享抽象和值类型（OperationResult、PromptMessage、配置模型、异常体系等） | 无 |
| 资源 | a2a-t-resources | 打包的提示词资源和加载器（场景定义、槽位Schema、模板、系统提示词） | a2a-t-core |
| LLM | a2a-t-llm | LLM Provider集成（适配器接口、OpenAI兼容适配器、会话管理、结构化生成） | a2a-t-core |
| 提示词 | a2a-t-prompt | 提示词模板、渲染、Schema校验（场景识别、槽位提取、模板渲染、语义校验） | a2a-t-core, a2a-t-resources, a2a-t-llm |
| 协商 | a2a-t-negotiation | 协商工作流和状态处理（四种协商类型、状态机、存储接口、Handler注册） | a2a-t-core |
| 客户端 | a2a-t-client | 客户端门面（A2ATClient），提供任务提示词生成和客户端协商能力 | a2a-t-core, a2a-t-llm, a2a-t-prompt, a2a-t-negotiation |
| 服务端 | a2a-t-server | 服务端门面（A2ATServer），提供任务提示词合规校验和服务端协商能力 | a2a-t-core, a2a-t-llm, a2a-t-prompt, a2a-t-negotiation |
| 示例 | a2a-t-sample | 端到端示例，集成A2A Java SDK演示完整客户端-服务端交互流程 | a2a-t-client, a2a-t-server |

> **说明：**
>
> - 大多数集成场景仅需引入`a2a-t-client`或`a2a-t-server`，Maven会自动传递依赖所需的其他模块。
> - 通过BOM引入版本管理，可避免模块间版本不一致问题：
>   ```xml
>   <dependencyManagement>
>       <dependencies>
>           <dependency>
>               <groupId>net.openan.a2a-t.sdk</groupId>
>               <artifactId>a2a-t-bom</artifactId>
>               <version>1.0.0</version>
>               <type>pom</type>
>               <scope>import</scope>
>           </dependency>
>       </dependencies>
>   </dependencyManagement>
>   ```
> - `a2a-t-sample`为可选模块，不作为SDK制品发布，仅供开发参考。

### 配置项配套说明

**表 6** a2a-t-sdk-java v1.0.0版本配置项说明

| 配置分类 | 配置项 | 默认值 | 说明 |
|---------|-------|-------|------|
| 提示词运行时 | A2AT_LANGUAGE | en-US | 提示词资源语言，支持zh-CN、en-US |
| 提示词运行时 | A2AT_PROMPT_SOURCE_TYPE | local_file | 提示词资源来源类型 |
| 提示词运行时 | A2AT_PROMPT_RESOURCE_LOCAL_ROOT_DIR | .env文件所在目录的父目录 | 自定义资源根目录路径 |
| 合规校验 | A2AT_PROMPT_COMPLIANCE_ENABLED | false | 是否启用服务端合规校验 |
| 合规校验 | A2AT_PROMPT_COMPLIANCE_GUARDRAIL_PROVIDER | noop | 护栏提供者（当前仅noop） |
| 合规校验 | A2AT_PROMPT_COMPLIANCE_GUARDRAIL_TIMEOUT_SECONDS | 10.0 | 护栏请求超时（秒） |
| 合规校验 | A2AT_PROMPT_COMPLIANCE_GUARDRAIL_POLICY_ID | （空） | 护栏策略标识 |
| 合规校验 | A2AT_PROMPT_COMPLIANCE_GUARDRAIL_ENDPOINT | （空） | 护栏服务端点 |
| 合规校验 | A2AT_PROMPT_COMPLIANCE_GUARDRAIL_REGION | （空） | 服务区域 |
| 合规校验 | A2AT_PROMPT_COMPLIANCE_GUARDRAIL_CREDENTIALS_REF | （空） | 凭证引用 |
| LLM运行时 | A2AT_LLM_PROVIDER | openai_compatible | LLM提供者名称，支持openai_compatible和local_rule |
| LLM运行时 | A2AT_LLM_MODEL | （空） | 模型名称 |
| LLM运行时 | A2AT_LLM_API_KEY | （空） | API密钥（必填） |
| LLM运行时 | A2AT_LLM_BASE_URL | （空） | API基础地址 |
| LLM运行时 | A2AT_LLM_MAX_TOKENS | 2048 | 最大Token数 |
| LLM运行时 | A2AT_LLM_TEMPERATURE | 0.2 | 采样温度 |
| LLM运行时 | A2AT_LLM_TIMEOUT_SECONDS | 30.0 | 请求超时（秒） |
| LLM运行时 | A2AT_LLM_HISTORY_WINDOW | 12 | 对话历史窗口消息数 |
| LLM运行时 | A2AT_LLM_SESSION_MAX_TOTAL | 300 | 最大会话总数 |
| LLM运行时 | A2AT_LLM_SESSION_MAX_PER_PROVIDER | 100 | 单Provider最大会话数 |
| 协商 | A2AT_NEGOTIATION_STATE_STORE_TYPE | in_memory | 协商状态存储类型 |

> **说明：**
>
> - 所有配置项通过.env文件管理，配置模板见`package_data/env.example`。
> - SDK不自动发现.env文件，需由调用方通过`new A2ATClient(Path envPath)`或`new A2ATServer(Path envPath)`显式传入.env文件路径。
> - A2AT_LLM_API_KEY为必填项，须配置有效的LLM服务API密钥。
> - A2AT_LLM_BASE_URL可替换为任意兼容OpenAI协议的服务端点，接入不同的模型服务仅需修改此配置和对应的API_KEY。
> - 合规校验相关配置仅影响服务端行为，客户端无需关注。
> - A2AT_PROMPT_COMPLIANCE_GUARDRAIL_PROVIDER当前仅支持noop（透传），aws_bedrock和azure_content_safety为预留扩展接口，暂未实现。

### 协议标准配套说明

**表 7** a2a-t-sdk-java v1.0.0版本遵循的协议标准

| 标准 | 版本 | 说明 |
|-----|------|------|
| IG1453 A Structured Prompt of Agent to Agent Protocol for Telecoms (A2AT) | v1.0.0 | TM Forum发布的A2A-T结构化提示词协议标准 |
| IG1453 Agent to Agent Protocol for Telecoms (A2AT) | v2.0.0 | TM Forum发布的A2A-T协议标准，涵盖协商扩展 |

> **说明：**
>
> - a2a-t-sdk-java为A2A-T协议标准的Java版参考实现SDK，任务提示词格式和协商流程遵循上述协议标准。
> - A2A-T协议是A2A（Agent-to-Agent）协议在电信领域的扩展，由TM Forum标准化发布。
> - 协商上下文传递遵循A2A项目电信扩展URI约定，通过`https://projects.tmforum.org/a2aproject/telecommunication/extensions/DATA-NEGOTIATION-T/v1`键在Task.metadata中携带。


# CVE漏洞

本版本是OpenAN第一个发布版本，没有CVE披露漏洞。

# 源代码

OpenAN的代码仓库地址：<https://github.com/project-openan>

# 参与贡献

作为OpenAN的用户，您可以通过多种方式协助OpenAN社区，参与社区贡献的方法请参见[Contributing to OpenAN](https://github.com/project-openan/.github/blob/main/CONTRIBUTING.md)。

# 致谢

我们衷心感谢参与和协助OpenAN项目的所有成员。是你们的辛勤付出使得版本顺利发布，也为OpenAN更好地发展提供可能。
